json.extract! photo, :id, :name, :author_id, :category, :location, :lat, :long, :date_taken, :camera, :lens, :focal_length, :aperture, :shutter_speed, :iso, :description
json.photoUrl @photo.photoConnects.map { |file| url_for(file) }